{
  "scripts": {
    "build": "cd functions && tsc && cd..",
    "serve": "npm run build && firebase emulators:start --only functions"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.1",
    "firebase": "^10.14.1",
    "ts-node": "^10.9.2"
  },
  "devDependencies": {
    "eslint": "^9.13.0"
  }
}
